#include <stdio.h>
#include <stdarg.h>
#include "holberton.h"
/**
 * _printf - functions that print an produces output according to a format.
 * @format: is a character string.
 * Return: the number of characters printed
 */
int _printf(const char *format, ...);
{
}
